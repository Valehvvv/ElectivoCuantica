# Environment & secrets
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/

# SpinQ NMR isolated env (Python 3.9, see scripts/setup_spinq.sh)
.venv-spinq/

# IDE
.idea/
.vscode/
*.swp

# Notebook outputs / checkpoints
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Project outputs (reproducible)
results/*.png
results/*.csv
data/*.csv

# Per-run, per-backend result directories (results/<backend_mode>/<run_id>/)
results/*/

# Structured logs (JSON lines, one file per run)
logs/

# Frozen regression baseline (Phase 0) - must be tracked so
# tests/test_regression_baseline.py works on a fresh checkout.
!results/baseline_statevector.csv
